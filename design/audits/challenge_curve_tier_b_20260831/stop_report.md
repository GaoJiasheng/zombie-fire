状态：历史停工证据（已由 Owner 的章节分布/通关率带裁决解除；最终结果见 `final_report.md`）

# 挑战曲线第二步停工报告（Owner 第二次裁决后）

## 1. 099 新锚点已经解决

旧 `R=1.556/K=0.92` 公式已废止。以黄金法则一档满级、Tier B、v2 卡牌策略、固定十种子实测：

|K(099)|指数 speed/breach/mechanic|通关|胜局 Boss 中位|结论|
|---:|---:|---:|---:|---|
|1.6913|0.2 / 0.4 / 0.4|10/10|151.16s|通关率过高|
|5.0000|1.0 / 0.0 / 0.0|**9/10**|**201.02s**|同时命中 60–90% 与 150–220s|

因此 099 不再是阻塞项；满血胜利也不再判错，星级仍由现有基地余量规则决定。

## 2. 新的硬冲突

Owner 裁决要求每关挑战档位严格等于普通档位升一档（封顶 H），同时原合同要求 K 单调、各章参考样本 10/10。

### 早期升档下界

L005 普通为 L，因此挑战目标为 P。使用最有利于推进战线的全移速指数，在 `K=1.25` 下：

- 三种子最大推进：`0.360 / 0.412 / 0.637`；
- 三局基地均为 `100%`；
- 实际仍为 E/L，没有达到 P。

L010 同样目标 P，最大推进仅 `0.437–0.537`、基地全满。故要满足严格升档，早期 K 不能再降低，且实际还需要高于 1.25。

### L057 可胜上界

L057 使用规定的 L067 节奏样本。在同一 `K=1.25` 下穷尽三指数边界：

|指数分配|三种子通关|
|---|---:|
|全移速 `1/0/0`|0/3|
|全突破 `0/1/0`|2/3|
|全机制 `0/0/1`|2/3|

把 K 降到 `1.00` 后才恢复 3/3（基地 `65.52% / 99.76% / 100%`）。因此 L057 的 10/10 可胜上界低于 1.25；混合指数不可能优于三个边界同时消除 seed 1103 的失败。

### 数学冲突

单调 K 要求 `K(057) ≥ K(005)`：

- L005/L010 严格升档要求 `K(005/010) > 1.25`；
- L057 参考样本可胜要求 `K(057) < 1.25`。

两者无交集。这个缺口与 099 的新锚点、基地余量语义和 Boss 挑战带无关。

## 3. 全量三种子交叉检查

分段候选曾让 001–056、058–075、078、080–094 全部 3/3，但失败集中在：

- L057：0/3；
- L076、L077、L079：0/3；
- L095–098：各 2/3（随后通过把 099 压力延后到终局解决，非核心阻塞）。

将 061–080 降到 `K=1.0` 后，L076/77/79 均恢复 3/3，进一步确认它们也是普通关自身尖峰与全局单调曲线不相容，而非探针或参考构筑错误。

## 4. 需要 Owner 裁决

不需要新增基地承伤或治疗抑制杠杆。需要在以下现有合同中选择一个放宽：

1. 允许 K 继承普通模式局部起伏（不再全局单调）；或
2. “逐关严格升一档”改为章节代表点/分布约束；或
3. 允许 L057、L076/77/79 使用更高于 `N+10` / 当前免费毕业族的参考样本。

本工序未自行选择，未写逐关隐藏倍率，未修改普通 `data/levels.json`。

## 5. 原始证据

- `l099_old_ratio_anchor_10.json`
- `l099_runtime_anchor_k5000_10.json`
- `all99_candidate_3.json`
- `reference_floor_spikes_3.json`
- `reference_floor_early_targets_3.json`
- `k1250_speed100_early_3.json`
- `k1250_breach100_spikes_3.json`
- `k1250_mechanic100_spikes_3.json`

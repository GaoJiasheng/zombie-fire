# 设置页专用开关贴图修复 · 看图说明

状态：通过（六档实拍可辨，运行时贴图门禁与发布候选聚合全绿）

## 核对重点

1. “总声音 / Master Audio”和“触感 / Haptics”为开启态：青色轨道、旋钮靠右。
2. “减弱特效 / Reduce Effects”为关闭态：中性灰轨道、旋钮靠左。
3. 三个开关都使用同一套 120×48 轨道与 48×48 金属旋钮贴图；颜色仅用于表达状态，不使用血量 / 警示红。
4. 1080×1920、1320×2868、750×1334 三档中英文均需一眼辨别开关状态，标签保持 1.5 全局字号且不独立缩字。

## 报告更正

上一版 `ui_finish_round/看图说明.md` 将五项分散检查写成“门禁均完成”，但遗漏了 `check_runtime_ui_primitives`；当时该项实际因设置页四处扁平样式而失败，因此“验收全绿”不成立。本补丁以两张专用纹理替换四处扁平样式，并以 `check_release_candidate.py` 聚合复验作为最终结论依据。

## 终值

| 项目 | 终值 |
|---|---|
| 轨道运行时纹理 | `ui_switch_track.png`，120×48，透明、中性灰、20px 九宫格边距 |
| 旋钮运行时纹理 | `ui_switch_knob.png`，48×48，透明、浅色金属；实际显示 36×36 |
| 控件轨道 | 88×44；开启旋钮 x=48，关闭旋钮 x=4，位移 44px |
| 状态语义 | 开启=青色轨道+右置亮旋钮；关闭=灰色轨道+左置暗旋钮 |
| 标签 | 中英均保持全局 `FONT_SCALE=1.5` 字号，不再独立缩字 |

## 实拍

- `settings_switch_three_size_bilingual.png`：三尺寸 × 中英六屏总览。
- `screenshots/`：1080×1920、1320×2868、750×1334 的逐像素原图。

## 验证

- 设置页三尺寸 × 中英：6/6 截图与布局分析通过。
- `check_runtime_ui_primitives.py`：通过，`gameplay/meta/ui` 中无 `ColorRect` / `StyleBoxFlat`。
- `check_hardcoded_colors.py`：通过；上一轮六屏背景调色已登记为 `UiKit` 共享调色板常量，画面未改色。
- `validate_asset_pack.py` / `check_res_refs.py` / `check_visual_assets.py` / `check_contrast.py` / `check_app_store_ui_polish.py` / `check_localization.py` / `check_release_strings.py`：全部通过。
- `check_release_candidate.py`：通过（显式跳过已由本页六档实拍覆盖的窗口矩阵；全部静态、非视觉与 headless 门禁启用），末尾 `M1 smoke test passed`、`Release candidate check OK`。

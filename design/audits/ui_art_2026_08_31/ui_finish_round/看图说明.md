# 美术 / UI 收尾轮看图说明

状态：通过（授权文件域内的实改、截图与门禁均完成）

## 看图顺序

1. `before_after_key_screens.png`：五个关键屏的改前 / 改后纵向对照。重点看设置页开关、菜单 slogan、地图锁定措辞、收藏锁定加成、配装星徽刻度。
2. `three_size_bilingual_after_contact_sheet.png`：1080×1920、1320×2868、750×1334 三档 × 中英双语 × 菜单 / 地图 / 配装 / 收藏 / 设置，共 30 屏。
3. `zero_resources_contact_sheet.png`：地图 / 配装 / 收藏在金币、星星、经验均为 0 时的中英对照。主题边框与点击区域保留，只降低 0 值图标和数字的视觉权重。
4. `after_*/`：逐像素原图；`after_1080x1920_zero_resources/` 是零资源补充证据。

## 本轮终值

| 项目 | 改动 | 终值 / 结果 |
|---|---|---|
| 地图锁定措辞 | “未展开”改为“未解锁” | 英文仍为 `Locked`，中英语义对等 |
| 锁定芯片加成 | 锁定态“当前加成”改为“解锁后加成” | `Bonus After Unlock`；已拥有芯片仍显示“当前加成” |
| 零资源货币栏 | 只弱化 0 值图标与数字 | 图标 alpha 0.48；数字使用弱文本色 alpha 0.66；边框与交互不变 |
| 配装星级门槛 | 裸数字改为 1/2/3 星三段刻度 | 阈值仍只读 `StarRules`：<35% / ≥35% / ≥70% |
| 战术摘要配色 | 五彩强调收敛 | 数值/关卡=金，正向=绿，弱点/短板=紫 |
| 六屏背景层次 | 同底图按屏定色 | 菜单中性、地图暖褐、配装钢蓝、收藏青灰、商店铜褐、设置冷蓝 |
| 设置页二元项 | 总声音、减弱特效、触感改为开关 | 文本不缩字；开=青色轨道，关=灰色轨道 |
| 主菜单 slogan | 从 Logo 下方移到行动区上方 | 活动位留在中段，slogan 与按钮组形成一体 |
| SE 配装安全区 | 压缩容器节奏，不缩字号 | 中英 750×1334 均零越界；英文原缺口 142px 已消除 |
| combo 测试 | 截图先等 0.18s bump 完整结束，再测静止帧 | 静态光心目标 1465.5px、容差 ±1px；连续 3 轮均通过 |

## 当前 main 已有、未重复施工

- 设置 AccessibilityRow 同行字号、collection 左安全区、地图 Boss / Challenge 徽章裁切：本轮开工基线已通过对应视觉检查。
- `skill_multishot` 4/5 级英文、角色“下一级”、三名锁定角色差异化预告：当前 main 已存在。
- 配装武器卡已经使用 alpha 裁切后的横向大展示尺（免费 388×252、付费 410×296），实拍不存在描述中的下半空洞，因此未叠加多余底座。

## 文件域内未擅改项

- 战斗伤害数字 / 威胁标记重叠位于 `gameplay/hud/` 与 `gameplay/enemy/`；卡牌详情装饰线和“近线威胁”源字符串位于 `gameplay/battle/`，均不在本轮授权文件域，未改。
- HUD 的蓝色宝石是经验条 `XpIcon`，数值已在相邻 `XpBar/Label` 中显示，属于有意的“图标 + 条内数值”，不补重复数字。

## 文案中英对照（Owner 复核）

| 中文 | 英文 |
|---|---|
| 未解锁 | Locked |
| 解锁后加成 +%s | Bonus After Unlock +%s |
| 防线完整度 | Base Integrity |
| 总声音 | Master Audio |

## 验证

- 三尺寸 × 中英核心矩阵：30/30 通过；零资源补充：6/6 通过。
- `m1_smoke_test.gd`：通过，零 ERROR。
- `check_visual_assets`：通过（1092 个战斗纹理文件）。
- `check_contrast`：通过。
- `check_app_store_ui_polish`：通过（32 个深度渲染替换）。
- `check_localization`：通过（641 exact messages，549 reusable terms，1163 runtime Chinese sources）。
- `check_release_strings`：通过。
- `battle_combo_2/11/100`：同一工作树连续三轮均通过。

注：关键改前图复用 2026-08-30 的逐像素基线；配装改前图来自当日 iPhone 16 Pro 实拍并在 contact sheet 中等比适配。所有改后原图均由当前 main 的离屏管线重新生成。

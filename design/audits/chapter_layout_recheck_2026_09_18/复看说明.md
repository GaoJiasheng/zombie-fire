# 第一战区排布复查

## 旧图和现状

Owner截图来自桌面16号目录的9月14日全量包，不是本轮工作区的当前排布。当前基线55cbff28已含a80b2820的关卡标题放大、推荐/弱点同行、左上固定返回；但介绍卡仍强制400高、故事与目标被弹性留白分开，英文长信息触发整组按钮下移，确实还有可优化之处。

本轮重新拍摄修前48张，不把历史截图当作当前状态。左图是本轮开始的最新代码，右图才是本轮进一步优化结果。存档/语言/尺寸/主题在每一对截图中相同。

## 本次修补

1. 介绍卡使用整宽正文，故事和目标连续排列，不再左右窄栏加大块空白；页顶已经显示战区名，卡片内不再重复一次。
2. 进度在左、返回在右，共享底部一行。第一战区卡片由400降至310画布像素；较长章节正文可继续撑高，无行数截断。
3. 英文关卡名与普通/挑战保持并排，推荐和弱点信息较长时使用全宽底行。变体徽章移到关卡名下方，避免侵占标题宽度。中文信息放得下时保留紧凑的左右布局。
4. 标题仍是30基准字号；全局FONT_SCALE=1.5不变。没有缩小任何模式按钮、改动普通/挑战星级或解锁行为。
5. 返回仍是原生286×80；既有顶部>=172、底部>=56的断言保留，未降低门槛。滚动区域外的左上返回继续保留。

无文案语义改写：中英故事、目标、关卡名、推荐战力、弱点和按钮文案沿用原串。仅消除重复战区标题，不删除世界观信息。

## 人工复看

重点看黄金中文1080×1920、黄金英文750×1334、默认中英标准屏，以及高屏、末战区/未通关对照。当前标题、推荐/弱点、模式按钮各有明确位置，没有在复看的组合里发现相互覆盖。英文长关卡名仍可能自然换为两行，这是保留授权字号与完整文案的结果。

第一屏底部出现下一关的半张卡是滚动边界，不是卡内文字被截断；向下滚动可完整查看。上部无限尸潮和装备快捷栏未改，本轮不新增折叠功能。

## 验收证据

- 修前后各48张：五主题×中英×三尺寸的第一战区30张，默认主题末战区/未通关/总览18张；原始尺寸1080×1920、1320×2868、750×1334，带安全区压力、静默离屏、只读隔离存档。
- `layout02.log`为最终专项：99关×五主题×三尺寸×中英=2970个关卡布局组合，含正文完整性、元信息同行、按钮和徽章互不重叠、固定返回和返回交互。
- 初版测试按节点名称前缀计数，重复打开第一战区时Godot对新旧同名节点临时去重，导致漏检300项；已改为按关卡结构识别，并硬断言2970项。不要把`layout01.log`当作全覆盖证据。
- 聚合门禁见`release_candidate.log`，含runtime UI primitives、视觉资产、对比度、本地化和m1；新增m1正文/页脚分离与进度/返回同行断言，旧阈值未改。
- 图片原生文件、像素尺寸与哈希由`deliver.py`校验后复制到桌面25号目录。不是重新跑全游戏大包，也不是已发布的TestFlight版本。

## 边界

运行时只动`meta/map/map.gd`；工具仅m1增加回归断言与等待布局稳定帧，其余为专项和证据。data/、assets/、gameplay/未改，免费四段指纹与本地main一致。未读写主仓库代码；聚合资产检查仅沿用已批准的历史源素材只读例外。

不push、不合并主线、不打包。使用独立HOME，不修改或删除Owner共享存档。桌面入口：`25_战区布局复查_2026_09_18/00_第一战区前后对照.html`。

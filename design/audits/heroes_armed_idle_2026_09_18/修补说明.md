# 另外三名角色：持枪待机修补

状态：通过。86张最终截图零审计问题，288轮状态切换零失败，完整非窗口发布门禁60项及m1通过。

## 发现与处理

钢铁先锋、火焰少年、冰霜少女都有与闪电少女相同的问题。普通武器使用的旧待机和受击素材没有双手握枪，开火时才切到真正持枪的姿势；不是武器装备或射击数值丢失。

将上一轮已验证的持枪待机规则扩展到这三人：复用每个角色、每把武器已有的第一张持枪准备帧（没有枪口火焰），保留原有呼吸、受击运动。四名角色现在规则一致。付费武器本来就有持续持枪母版，仍走原路径；没有重新生成或覆盖任何素材。

## 看图说明

桌面 `22_全角色持枪待机_2026_09_18/00_三角色前后对照.html` 提供三组前后放大对照。每组依次为修前空手待机、修后持枪待机、保留的开火姿势，点击可查看原生整图。

重点看双手与武器的连接、停火时枪是否消失、人物身体与脚底是否突变。网页仅放大展示原始截图，不修改截图像素。

最终截图86张：三角色×八普通武器×待机/开火48张；散弹枪×四个额外主题外观×三角色×两状态24张；散弹枪SE及高屏补12张；闪电少女回归2张。另保留3张修前同场景截图。覆盖1080×1920、1320×2868、750×1334；不是所有主题、武器、尺寸的全组合。此次不改文字，截图使用中文。均为静默离屏渲染，非真机实测。

## 验证与边界

- 四角色×八普通武器×三瞄准方向×连续三次开火/停火/受击，共288轮状态切换零失败，枪口坐标逐一比较不变。
- m1常规冒烟增加所有普通角色/武器的持枪待机、受击和对应身体锚点断言，不放宽旧断言。
- 唯一运行时差异为动画加载函数中去掉Volt限定；44个确定性相关函数逐字未变。
- data/与assets/无变化，免费侧四段指纹与本地main一致；无数值、弹道、射击频率、瞄准改动，无英文改写。
- 截图及测试使用隔离HOME、只读捕获，未写入或删除Owner共享存档。未push、未合并主线、未打TestFlight。

验收原始记录见 `transitions.log`、`scope.json`、`after.log` 与 `release_candidate.log`；最终核验汇总见交付后生成的 `result.json`。

最终验收命令：`ZOMBIE_FIRE_SKIP_WINDOWED_VISUALS=1 ZOMBIE_FIRE_SOURCE_REFS_ROOT=/Users/gavin/work/zombie-fire ZOMBIE_FIRE_TEST_HOME=<本审计目录>/test_home python3 tools/check_release_candidate.py`。使用已授权的主仓库历史素材只读例外，未读取主仓库在制代码。日志为 `M1 smoke test passed`、`Release candidate check OK`，无ERROR或check failed。

过程记录：首轮两次截图在进程退出时出现`1 resources still in use at exit`，虽然退出码为0、图像审计为空，仍未直接交付。原始截图/记录归档到`after/verification/exit_cleanup_retry/`，相同源代码重拍两张；最终交付校验同时检查每张日志无ERROR，见`after_retry.log`。

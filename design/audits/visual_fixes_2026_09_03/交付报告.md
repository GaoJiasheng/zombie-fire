通过：F-X1～F-X4 与弹窗复核完成；最终聚合门禁通过，M1 零 ERROR。

## 分支与边界

- 工作区：`/Users/gavin/work/zf-visualfix`；分支：`codex/visual-fixes`。
- 同步基线：本地 `main` 快进至 `648b040e`，无冲突；基线完整聚合门禁通过。
- 最终运行时代码 HEAD：`9a4174dc`；后续交付提交只归档报告与验收证据。
- 本轮提交范围：`648b040e..9a4174dc`，另含最终证据提交；未 rebase、未 push、未打包 TestFlight。
- 运行时只改 `meta/settings/settings.gd`、`meta/collection/collection.gd`、`meta/loadout/loadout.gd`；另增加 M1 断言和设计进度 / 证据。
- `gameplay/`、`core/`、`data/`、`ui/` 相对同步基线零差异；确定性路径、散弹枪阶梯 / 扇形 / 瞄准锚均未触碰；所有原有断言阈值保留。
- 主仓库在制代码未读取或改动；仅按 Owner 的“好”授权，在资产包验收时只读历史素材源文件。

## 逐项结果

| 项目 | 提交 | 改动及终值 | 证据 |
| --- | --- | --- | --- |
| F-X1 | `53809e2a` | 信息正文按换行后的自然高度展开；英文需要 215px，实际 227px，短屏仅回收行间留白 | 6 组前后图，18 组信息状态断言 |
| F-X2 | `91519ea2`、`9a4174dc` | 名称智能换行，等级独立纹理徽章；英文授权 37 号，中文授权 43 号；徽章复用现有本地化格式 | 四主题双语顶部 / 付费区，共 16 组前后图 |
| F-X3 | `f2ed1cce` | 被动 / 专属技能先于升级预览；首屏按完整信息块留白；202px 固定操作栏对应 246px 滚动尾部空间 | 26 组前后图；四角色双语四高度共 32 组首屏 / 滚动到底断言 |
| F-X4 | `9fffe1a8` | 弱点自然宽度，关卡取剩余空间；英文普通关卡值需 369px、可用 491px，挑战需 392px、可用 492px | 6 组前后图；普通 / 挑战共 12 组断言 |
| A5 | `4d1e5796` | 只复核主线按钮顺序；立即应用整套在上，逐个角色换装在下；无文字裁切 | 中英标准 / 高屏共 4 张图 |

武器目录保留既有“所有卡片、中英文同高”的合同，标题区按最长实际名称测量；因此短标题上方留白比旧版多。完整顶部图已附，便于 Owner 复看这一布局取舍。

## 验收

| 命令 / 方式 | 结果 |
| --- | --- |
| `ZOMBIE_FIRE_SKIP_WINDOWED_VISUALS=1 ZOMBIE_FIRE_SOURCE_REFS_ROOT=/Users/gavin/work/zombie-fire python3 tools/check_release_candidate.py` | 通过，日志无 `check failed`；见 `verification/final_rc.log` |
| `godot --headless --path . --script res://tools/m1_smoke_test.gd` | 最终完整执行通过，包含全部新增断言，零 ERROR |
| `godot --headless --path . --script res://design/audits/visual_fixes_2026_09_03/verification/round3_smoke.gd` | 通过；新增正文 / 信息块 / 摘要布局断言 |
| `python3 tools/check_release_strings.py` / `check_localization.py` | 最后一次徽章本地化修正后通过 |
| `capture_round3.py` 调用原 `check_visual_screens.capture()` / `analyze()` | 58 张改后 / 弹窗复核图全部 `capture_exit=0, issues=[]`，原生尺寸精确匹配 |
| `python3 tools/free_side_fingerprint.py .` 与同命令追加 `main` | 四段逐字相同 |
| `git diff 648b040e -- gameplay core data ui` / `git diff --check` | 零冻结域差异 / 通过 |

聚合门禁覆盖并通过：validate_asset_pack、validate_data、check_res_refs、check_level_pressure、simulate_card_director、Godot 启动、存档完整性、主题、本地化、M1，以及 visual_assets / contrast / app_store_ui_polish / runtime_ui_primitives / localization / release_strings 等全部非窗口检查。

四段指纹：

```text
levels=2669c4acdca69fbf weapons=bdd10432c49bebed economy=2a027b2ed076bebd fixture=bc313dff5d8b033d
```

首轮基线误带截图只读配置导致存档写入测试失败，纠正配置后基线通过；首轮最终聚合发现徽章直接写英文等级格式，改为本地化模板后重新执行。失败历史单独保留，不充作通过证据。

未重跑全量 1200 屏或 99×10 平衡探针；本轮按受影响路由复拍，不重新计算战力或星表。

## 证据与交接

- 仓库：`design/audits/visual_fixes_2026_09_03/`，优先打开《看图说明.md》。112 张原生截图、54 张全像素左右对照、8 张总览图。
- 桌面副本：`/Users/gavin/Desktop/zombiefire_screenshots/12_视觉第三轮/`；由 `verify_delivery.py` 同步并逐文件核对 SHA-256，记录在 `verification/delivery_integrity.json`。
- 英文文案未精简或改写，中文同样未改；没有需要 Owner 审批的新文案语义。
- 已按要求永久清理共享测试 `save_main.json` / `save_backup.json`，未保留额外副本；未删除其它存档或资产。完整验收后未再启动 Godot。
- 收尾仅归档本轮报告与验证日志，运行时代码没有遗留未提交改动；最终归档提交 HEAD 与清洁工作树确认见交付回复。
- 后续由 Fable 合并此分支并按主线在制品处理冲突；本会话不操作主仓库或推送。

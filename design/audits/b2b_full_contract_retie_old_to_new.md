# B2b 全量合同重钉：旧 → 新

日期：2026-08-29

本链只重生成派生合同、合并固定帧证据并切换正式攻速默认档；不改移动速度、基地受伤、
全局曲线、星级阈值或付费武器数据。

| 合同 / 钉值 | 旧值 | 新值 | 依据 |
|---|---|---|---|
| 正式攻速默认档 | `control` | `tier_b` | Owner 冻结的 B2 档；`control` 仍保留为不可变算术对照 |
| 第 6 章完整数据哈希 | `96a05ab…58609` | `898de3de…ec0cd` | 全量 clear requirement 重生成后的 051–060 精确数据 |
| wave-pressure 前十关 authored 哈希 | `0c9ebe12…3d82` | `bb54ff97…e78b` | B2b 001–010 合并结果 |
| B2a 061–099 `run_xp_budget` 总量 | `95310` | `95741` | 工序 B solutions 合并后的逐关 authored 总量 |
| 050 类别池 smoke | 无 floor | `compatible_crowd` | 第 5 章最终 100/100 固定帧证据 |
| Tank Titan 抗性 smoke | 写死 50% | 读取数据行 52% | 测试跟随既有 Boss 数据，不修改 Boss |
| 099 静态总 HP 峰值 | 硬失败 | 报告项 | 001–099 已全部由固定帧证据接管；Boss 阶段带仍是硬合同 |
| 固定帧星级汇总 | 局部 fixture 混合 | 96×3★ / 3×2★ / 0×1★ | 99 关各自十种子基地中位数 |

## 权威证据拼接

- 第 1–4 章仅替换实际被 B 修补的关；010 / 020 保留原证据，015 使用正确 20–35 秒重解，040 使用 2.06% 最差余量复验。
- 第 6 章替换 052 / 057 / 059；第 7–10 章替换 23 个有效 repairs；092 / 098 为 no-op，继续使用原证据。
- 所有章节证据仍保持每关十种子且全胜；全 99 关 clear requirement 已重生成，97 关功率走廊保持 `R=[1.02,1.38]`，满级免费毕业族 99/99 在上限内可通关。

## 已知终验项

`check_endgame_balance.py` 依据新拼接证据列出 065 Boss `61.5s`（目标上限 60s）与
090 Boss `91.8s`（目标下限 100s）。两项不在本合同链改关；按 Owner 流程在星表审批后的
99×10 终验中列清单并修复。039 / 084 的严格静态包络超限继续作为报告项，十种子安全优先。

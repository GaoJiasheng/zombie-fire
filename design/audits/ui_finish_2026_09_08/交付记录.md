# 通过：28项UI修复与全量截图验收完成；真实支付待iOS沙盒

## 版本与边界

- 分支：`codex/visual-fixes`；工作树：`/Users/gavin/work/zf-visualfix`。
- 已提交代码终点：`b32841b7ed7aaf52aa1970e80bed28ec844ab0ef`。
- 提交范围：`02505b24..b32841b7`，23 个分项代码/素材/工具提交；逐个文件和提交说明见 `verification/commits.json`。最后四项为取消新增武器染色、商店卡内边距、宠物冷却精度及资源门禁目录纠正。
- 未 push、未 rebase、未合并主仓库、未打 TestFlight。已安装手机构建不会自动包含这些改动。
- 23项运行时/工具修正已提交，另有一项收尾文档与回归证据提交；交付HEAD与工作树状态见 `verification/final_git_state.json`。大体积截图留作本地交付，不纳入Git；原有其它未跟踪在制品保留，没有批量加入提交。
- 未改战斗数值、弹道、瞄准、FONT_SCALE=1.5、章节入口286×80；44个确定性相关函数逐函数一致。core/save、core/commerce 与数值数据零差异。
- 免费侧四段指纹与本地 main Git 对象一致：levels `2669c4acdca69fbf`、weapons `bdd10432c49bebed`、economy `2a027b2ed076bebd`、fixture `bc313dff5d8b033d`。没有重跑99×10。
- 主仓库计划内访问为Owner批准的历史源图只读验收；另发现旧资源检查脚本硬编码主仓库根目录，曾非预期只读扫描其代码引用。已改为本隔离树重验454个引用并纠正报告，不沿用旧结果。主仓库无写入，其它worktree未访问，详见 `verification/validation_root_note.md`。

## 实施结果

原报告28项：首轮12项保留，本轮剩余16项均已实施并复验，另修4条设置安全区压力警告和真实支付消息误改写。逐项前后图见 `UI收尾对比报告.html`；人工抽样记录见 `人工复核.md`。

本轮重点为标题/等级徽章、图标加文字的组合居中、霓虹框体可见中心、完整英文选卡、短详情高度、图鉴信息层级、黄金图标真实透明背景。正文保持左对齐，不把所有文本强制居中。

Owner 于2026-09-13批准的两个收尾项目已完成：隐私和三项技能的过时叙述删除；两张黄金图标常规抠图/边缘/原生尺寸整理。中英逐条见 `文案与展示变更.md`，素材来源与旧图归档见 `素材说明.md`。

## b32841b7上已通过的检查

- `ZOMBIE_FIRE_SKIP_WINDOWED_VISUALS=1 ZOMBIE_FIRE_SOURCE_REFS_ROOT=/Users/gavin/work/zombie-fire ZOMBIE_FIRE_TEST_HOME=<本目录 verification/test_home> python3 tools/check_release_candidate.py`：60项全部通过，结尾 `Release candidate check OK`。包含 m1 零ERROR、runtime UI primitives、visual assets、contrast、UI polish、localization、release strings、数据/资源引用等；不是只挑5项检查。
- 通过项目已有隔离测试存档入口运行 `ui_finish_test.gd`：`UI finish regression: 0 failures`，日志 `verification/ui_finish_test_supplement03.log`。覆盖五主题中英武器标题/等级、全部武器复用原素材、286×80入口、真实消息保真、三尺寸最长选卡、霓虹纹理状态、四系列全装备卡内边距，以及中英文宠物小数/整数冷却。
- `python3 verification/verify_scope.py`：冻结路径、数值与指纹、逐字共享说明、旧图可恢复性全部通过。
- `pilot07`：霓虹菜单与暂停，中英三尺寸12张 issues=[]，已目视确认可见边框及主次层级。

## 全量图证结果

- `final02` 在固定提交722fedc6上完成1007张合格图后按Owner最新要求暂停；已核对源码未变。保留其中793张不受补修影响的图，最新版本完成其余1441张，覆盖全部2236逻辑用例/2234张独立截图。报告明确两版组合证据，不称旧批全部完成。
- 最终2234张全部核对原生尺寸、SHA256、退出状态与日志：运行时0条、图像0条、无ERROR。原报告79张/226条提示已消除；旧批26张退出资源清理错误已排除重拍，诊断留档，未计作合格图。
- 生成126张五主题同屏对照、28项前后对照及全部截图可搜索索引；机器全量与人工抽样范围分别记录，未冒称逐张人工检查或真机实拍。报告桌面/窄屏验证见 `verification/report_layout_qa.json`，桌面副本逐文件校验见 `verification/desktop_copy_verification.json`。
- 商店卡18px内边距和宠物10.5秒显示精度已修，Owner取消的新增武器主题染色已撤回。pilot08五张实拍检查和目视通过；最新完整聚合门禁通过（`verification/release_candidate_supplement.log`），本树资源引用454项通过。全部商店/宠物/配装场景均纳入最新版本续拍。

桌面交付：`/Users/gavin/Desktop/zombiefire_screenshots/15_UI收尾_2026_09_13/`。优先打开 `UI收尾对比报告.html`，再按需看 `同屏对照索引.html` 与 `全部截图索引.html`。

## 仍需真实设备的唯一验证项

DEVICE-01的代码修复和消息断言已通过，但真实 StoreKit 购买/取消/待批准/失败/恢复，以及系统语言表现仍需 iOS 沙盒确认。本机离屏渲染不能冒充真机交易完成。

所有 Godot 测试与截图串行，截图静音、离屏、只读，使用独立测试存档路径；真实用户存档未读取、写入或删除。

# 资源引用门禁目录纠正（2026-09-14）

发现 `tools/check_res_refs.py` 第5行将根目录硬编码为 `/Users/gavin/work/zombie-fire`。此前聚合检查调用它时，实际只读扫描了主仓库的引用，而不是本隔离树；这是非预期的边界偏差，不能把那次结果当作本树的资源引用验收。扫描未写入主仓库。

修复只改根目录解析为 `Path(__file__).resolve().parents[1]`，不改匹配规则、扫描范围、失败标准或任何产品文件引用。当前隔离树重验454个引用，结果 `res:// references OK`（旧日志为446个）。

之后须重跑完整聚合门禁，最终报告采用新日志。已在本树工具目录排查同类硬编码root，仅发现这一处。主仓库历史源图只读验收仍沿用Owner已授权的SOURCE_REFS_ROOT例外；无主仓库写入、无其它worktree访问。
